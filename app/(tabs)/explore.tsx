import { Image } from "expo-image";
import { Platform, StyleSheet, View } from "react-native";

export default function TabTwoScreen() {
  return <View>This is explore tab and thing for git</View>;
}
